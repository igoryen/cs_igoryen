@model IEnumerable<igoryen.Models.Cancellation>

@{
    ViewBag.Title = "Found";
}

<h2>Found</h2>

<p>
    @Html.ActionLink("Find New", "Find")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Message)
        </th>
        <th>
            User
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Message)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CancellationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CancellationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CancellationId })
        </td>
    </tr>
}

</table>
@ViewBag.debug0<br />
@ViewBag.debug1<br />

